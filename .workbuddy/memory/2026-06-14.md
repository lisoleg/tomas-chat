## 2026-06-14 修复记录
- **EMLGraphVisualization.tsx "Invalid hook call" 错误**: 第341-342行在 useEffect 回调内部调用 React.useRef() → 移至组件顶层
- **知识列表混合问题**: 主滚动区「知识列表」是概念+关系混合的 → 拆为两个独立区域（概念列表绿色/关系列表蓝色）
- **列表项不可点击**: bridgeState 顶点/边和蒸馏结果缺少 onClick → 全部添加点击选中→跳转图谱
- **183节点图谱拥挤**: 三级优化：>80节点 radius=8/scale=0.45/边透明度0.06；40-80节点中等；<40保持
- **图谱高度不足**: 从固定620px改为动态计算 72%视口高度（最低600px）
- **ReferenceError: nodeCount TDZ 崩溃**: 第257行用nodeCount但声明在第268行 → 提前用vertices.length声明
- **伪概念泄漏**: "1543年5月24日"等日期/数字/度量值被当概念 → 后端加强prompt+is_pseudo_concept()后处理过滤，前端同步添加isPseudoConcept()过滤器

## 04:00-04:30 可视化UI修复 + OwnThink数据初始化
1. **对话重命名** — Sidebar ✏️按钮 + 内联输入框 + Enter/Esc/blur 交互
2. **图谱节点黄色字体** — #fde047 替代 #1e1b4b
3. **边5色按关系类型** — associator_flag 映射 (0=indigo, 1=amber, 2=emerald, 3=pink, 4=violet)
4. **自动缩放优化** — 小图2.0x~大图0.45x
5. **语料标签** — ≤50节点时显示 corpusName（浅紫色#a78bfa）
6. **关系列表选中高亮** — selectedRelationKey state + bg-indigo 边框
7. **关系列表排版重构** — 双行布局：源—类型→目标 / 权重+操作（w→权重）
8. **外部同步保留corpusName** — graphData同步时从bridgeClient传递
9. **旧数据清空** — DistillPanel外部同步后清空knowledgeItems/corpusEntries；App.tsx autoLoadAll清理localStorage
10. **App.tsx精简为只加载ownthink_sample.eml**
11. **根因**: DistillPanel useState初始值在App.tsx清理localStorage之前执行，导致旧数据先被读入

## 07:08 修复"点击概念没看到图谱"问题
- **根因**: `graphData` 状态始终为 `null` — 页面加载时没有任何代码从后端 `/api/knowledge/graph` 获取图谱数据来初始化 `graphData`
- **修复1 (DistillPanel.tsx)**: 在 `loadData` effect 中加载完知识条目后，同时调用 `GET /api/knowledge/graph?limit=5000`，将三元组转为 EMLGraphData 格式并 `setGraphData()`
- **修复2 (EMLGraphVisualization.tsx)**: 新增 `showAllByDefault` prop — 数据来自 API 时即使没选中任何项也全量显示图谱（不显示空状态）
- **新增状态**: DistillPanel 新增 `graphFromAPI` state；所有非 API 来源（EML上传/蒸馏/合并/外部同步）均重置为 false

## 07:23 修复图谱可视化4个UI问题
1. **连线太暗不可见** — 边透明度 0.06~0.4 → 0.12~0.5；线宽系数 1.5~2.5 → 2.2~3.0；所有搜索/重置/关系高亮处统一
2. **图谱靠左上角不居中** — 缩放平移公式错误（产生负偏移）：`cw/2*(1-1/S)*S` → `cw*(1-S)/2`
3. **节点圆太小看不见字** — 基础半径大图8→14px、中图12→16px、小图14→18px；标签显示阈值25→60节点；字体下限8→9px
4. **语料标记"蒸馏"+缺少标记警告** — API数据corpusName全null：跳过过滤不报警告；DistillPanel在graphFromAPI时不传selectedCorpus；标题改为`🌐 全部概念(N顶点·M边)`
